#include<stdio.h>

int main(){
    int radius = 8;
    float pi = 3.14;
    printf("Area of a circle is %f\n",pi*radius*radius);

    int height = 6;
    printf("Volume of a cyclinder is %f",pi*radius*radius*height);

    return 0;
}