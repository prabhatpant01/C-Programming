#include<stdio.h>

int main(){
    int a=1;
    //Loops are used to repeat similar part of a code snippet efficiently ... over and over again.
    //Simple hai mtlb compailer see ek kaam baar baar krwana too iske liye loop use hoata hai.
    
     printf("%d\n", a);
    a++;                   //) =----> 100 times   a++; is a increment operator.
    printf("%d\n", a);
    return 0;
}