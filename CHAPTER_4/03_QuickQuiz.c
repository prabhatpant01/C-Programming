// 10 to 20 natural number when initial loop counter is initialized to 0.//
#include <stdio.h>

int main()
{
    int i = 0;
    while (i <= 20){                       // while ke andr hee if aayega.
        
     if (i >= 10)
        {
            printf("The value of i is %d\n", i);
        }
        i++;
    }
    return 0;
}